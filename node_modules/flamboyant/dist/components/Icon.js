import React from "react";

import "../assets/css/polifont.css";

var Icon = function Icon(props) {
  var style = {
    fontSize: 26,
    color: "var(--principal-icons-color)"
  };

  if (props.color) {
    style = Object.assign({}, style, { color: props.color });
  }

  if (props.size) {
    style = Object.assign({}, style, { fontSize: props.size });
  }
  return React.createElement("i", { className: props.icon, style: style });
};

export default Icon;